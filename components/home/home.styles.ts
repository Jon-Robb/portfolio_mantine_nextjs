import { createStyles } from '@mantine/core';

export default createStyles(() => ({
    wrapper: {
        marginTop: '2rem',
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        height: '100vh',
        gap: '2rem',
        textAlign: 'center',
    },
}));
