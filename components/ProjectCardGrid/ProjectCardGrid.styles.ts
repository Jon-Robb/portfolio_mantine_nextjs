import { createStyles } from '@mantine/core';

export default createStyles(() => ({
    wrapper: {
        display: 'grid',
        gridTemplateColumns: '1fr',
        columnGap: '1rem',
        rowGap: '1rem',
        margin: '1rem',
    },
}));
