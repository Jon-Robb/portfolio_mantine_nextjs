import { createStyles } from '@mantine/core';

export default createStyles(() => ({
    slide: {
        display: 'flex',
        alignContent: 'center',
        justifyContent: 'center',
        height: '100%',
        width: '100%',
        alignItems: 'center',
        textAlign: 'center',
        borderRadius: '1rem',
    },
    container: {
        borderRadius: '1rem',
    },

}));
